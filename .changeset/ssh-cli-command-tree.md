---
"@moonshot-ai/kimi-code": minor
---

Add the `kimi ssh` command tree to open a remote machine's web UI through an SSH tunnel. Run `kimi ssh add <name> <[user@]host>` to save a connection, then `kimi ssh connect <name>` to open it in the browser.
