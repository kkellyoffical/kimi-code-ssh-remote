---
"@moonshot-ai/kimi-code": minor
---

Turn the built-in /ssh page into a remote console: pick an auth method (agent/default keys, identity file, or password) when adding a connection, optionally remember the password, enter it inline when a connection asks for one, and browse remote files and projects through the tunnel.
