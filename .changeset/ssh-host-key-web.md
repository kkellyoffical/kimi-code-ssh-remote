---
"@moonshot-ai/kimi-code": minor
---

Warn on the built-in /ssh page when a server's SSH host key changes: the page shows the new key fingerprint with a man-in-the-middle caution and offers a button to remove the old key and retry the connection. The page also gains a Delete button for removing a saved connection.
