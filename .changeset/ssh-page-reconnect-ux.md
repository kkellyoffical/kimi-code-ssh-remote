---
"@moonshot-ai/kimi-code": patch
---

Remove the file and project browsers from the built-in /ssh page (use Open to launch a connected remote's web UI) and show a reconnecting badge while a dropped connection recovers.
