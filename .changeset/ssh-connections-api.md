---
"@moonshot-ai/kimi-code": minor
---

Add SSH remote connections to the local server: register a remote machine, start Kimi Code on it over SSH, and use its web UI through a local proxy. Open the built-in management page at `/ssh` on the local server to add, test, and connect remotes.
